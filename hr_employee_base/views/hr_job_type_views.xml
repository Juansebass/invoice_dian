<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="hr_job_type_view_tree" model="ir.ui.view">
        <field name="name">hr.job.type.view.tree</field>
        <field name="model">hr.job.type</field>
        <field name="arch" type="xml">
            <tree editable="bottom">
                <field name="name"/>
            </tree>
        </field>
    </record>

    <record id="hr_job_type_action" model="ir.actions.act_window">
        <field name="name">Tipo de Cargo</field>
        <field name="res_model">hr.job.type</field>
        <field name="view_mode">tree</field>
        <field name="context">{}</field>
    </record>

    <menuitem
            id="hr_job_type_menu_action"
            name="Tipo de Cargo"
            action="hr_job_type_action"
            parent="hr.menu_human_resources_configuration"/>

</odoo>
